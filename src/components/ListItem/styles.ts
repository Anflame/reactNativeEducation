import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  listes: {
    marginTop: 10,
    padding: 5,
    textAlign: 'center',
    borderRadius: 3,
    backgroundColor: 'lightgrey',
    borderWidth: 1,
    width: '100%'
  },
});